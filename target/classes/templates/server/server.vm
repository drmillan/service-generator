## 
## SERVICIO - SERVICIO BASE ANDROID 
## 
/**
  ${serviceName}Service
  ${projectName}

  Created by Generator on 19/01/12.
  Copyright (c) 2012 Mobivery. All rights reserved.
*/
package ${packagename}.server.rest;

import javax.ws.rs.POST;
import javax.ws.rs.PUT;
import javax.ws.rs.GET;
import javax.ws.rs.Path;
import javax.ws.rs.PathParam;
import javax.ws.rs.Produces;
import javax.ws.rs.core.MediaType;
import javax.ws.rs.FormParam;

import ${packagename}.model.dao.*;
import ${packagename}.model.dto.*; 
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStream;
import java.io.InputStreamReader;
import java.net.URLEncoder;
import com.mobivery.server.DTORandomizer;


import org.json.JSONException;
import org.json.JSONObject;

import javax.ws.rs.Path;

@Path("/")
public class ${projectName}Rest
{
	// Generador de DTOs random
	DTORandomizer randomizer=new DTORandomizer();
	
	#foreach ($message in $messages) 
	/**
	 * MŽtodo ${message.method}: ${message.description}
	 * @param ${message.request.javaInstanceName} Petici—n al servicio
	 * @returns ${message.response.name} Respuesta del servidor
	 * @throws Exception Error de comunicaci—n / datos 
	 */
	 @Path("${message.path}")
	 @${message.typeUCase}
	 @Produces(MediaType.APPLICATION_JSON)
	public ${message.response.name} ${message.method}(
	${message.serverJavaRequestParams}
	) throws Exception
	{
		return randomizer.randomizeDTO(${message.response.name}.class);
	}
	#end	
}
